Para compilar el proyecto usar el comando mvn compile exec:java -Dexec.mainClass="lexico.Test"
En el archivo AnalizadorLexico.java esta la variable file, ahí se especifica el nombre del archivo
con extensión .p a analizar (Ejemplo file = fizzbuzz).
Se agregaron algunos lexemas no contemplados en el pdf de atomos ya que no se podía leer muy bien y había
algunos que consideramos que debían estar como los paréntesis o el operador ==.
